# Cloudflare Workers (static assets). Dashboard deploy command stays the
# default: npx wrangler deploy
name = "someday"
compatibility_date = "2026-07-30"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
